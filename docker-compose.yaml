version: '3'

services:
  vue-app:
    build:
      context: "./."
      dockerfile: "Dockerfile"
    ports:
      - 8080:8080
    volumes:
      - ./app:/src
    tty: true
    environment:
      - NODE_ENV=development
